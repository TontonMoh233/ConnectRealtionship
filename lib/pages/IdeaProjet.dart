import 'package:flutter/material.dart';

class IdeeDeProjet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Idées de Projet'),
      ),
      body: Center(
        child: Text('Contenu des idées de projets'),
      ),
    );
  }
}
